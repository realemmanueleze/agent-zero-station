# T13 tests (commit before the ship kit)

1. `LICENSE` is MIT.
2. `docs/CONTRACT.md`, `docs/ADDING_A_CHANNEL.md`, and `docs/DEPLOY.md` exist.
3. `Dockerfile` and `compose.yml` exist.
4. Compose smoke exits 0 when a parked fixture is present.
5. `.env.example` lists every config key the process reads.
