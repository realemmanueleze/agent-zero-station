# T26 evals

- `gate: merge` — `evals/suites/cockpit.worker-views.eval.ts`: mapped activity has a decision id; brief mentions waiting; no slack seed id.
- `gate: nightly` — browser Activity/Brief against a live worker (not CI).
