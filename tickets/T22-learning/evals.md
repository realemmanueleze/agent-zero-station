# T22 evals

- `gate: merge` — `evals/suites/learning.proposals.eval.ts`: proposal markdown exists; directives.md unchanged; no commit_send.
- `gate: nightly` — daily 03:00 job on a live host (not CI).
