# T24 tests (commit before the page)

1. `docs/PRIVACY.md` exists and states the kit is self-hosted.
2. Privacy HTML has no secrets, tokens, or mail bodies.
3. `/privacy` is linked from the cockpit shell.
4. Scoring turn still cannot call `commit_send`.
