# T17 evals

- `gate: merge` — `evals/suites/email.poller.eval.ts`: paste a mailbox, start live producers, park list has that account, model never sends.
- `gate: nightly` — live IMAP unseen on a real box (not CI).
