# T18 evals

- `gate: merge` — `evals/suites/loop.deep-agents.eval.ts`: live tools omit commit_send; recorded draft still parks; tenant A prompt has no tenant B secret.
- `gate: nightly` — live model key on the same fixtures (not CI).
