# T25 evals

- `gate: merge` — `evals/suites/boot.producers.eval.ts`: paste, restart worker, inbound parks for that account; model never sends.
- `gate: nightly` — live IMAP after `pnpm dev` (not CI).
