# T1 evals

Schema has no model. Recorded eval is a fixture-load smoke used later by T6.

- `gate: merge` — `evals/suites/schema.fixture-upsert.eval.ts`: loading `fixtures/demo.jsonl` twice yields one parked row.
- `gate: nightly` — none
