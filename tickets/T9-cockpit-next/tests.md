# T9 tests (commit before the Next.js cockpit)

1. `ParkCard` renders Approve, Edit, and Kill for a parked item.
2. `ParkCard` HTML never contains `STATION_CONTROL_TOKEN` or a Bearer token.
3. `registerParkRenderer` replaces the default card for a pack id.
4. `tokens.css` defines `--bg`, `--ink`, `--park-border`, `--mono`.
5. Command palette action list includes Approve, Switch pack, and Toggle theme.
