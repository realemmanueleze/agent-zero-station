# T15 evals

- `gate: merge` — `evals/suites/connections.add-source.eval.ts`: mock OAuth + two accounts on Channels HTML, denylist secrets, Approve still parked without a human.
- `gate: nightly` — live Gmail when the founder assignment is done (not CI).
