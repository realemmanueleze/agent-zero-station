# T11 evals

- `gate: merge` — `evals/suites/email.park-then-approve.eval.ts`: fixture mail parks, Approve calls transport once, model never sends.
- `gate: nightly` — live Gmail/IMAP when credentials exist.
