# T27 evals

- `gate: merge` — `evals/suites/loop.live-tools.eval.ts`: inbound parks with a draft; traces omit send; ledger search stays tenant-scoped.
- `gate: nightly` — live model key path (not CI).
