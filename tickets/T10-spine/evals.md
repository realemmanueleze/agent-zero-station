# T10 evals

- `gate: merge` — `evals/suites/replay.pack-switch.eval.ts` already requires pack switch to change the decision. Spine must keep that green with real pack modules.
- `gate: nightly` — none
